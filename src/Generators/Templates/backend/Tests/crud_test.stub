<?php

namespace [[testNamespace]];

use [[namespace]]\[[ModuleName]]Model;
[[UsersModelImport]]use Laravel\Sanctum\Sanctum;
use Tests\TestCase;

/**
 * Generated feature coverage for the [[ModuleName]] module.
 *
 * Authenticates as the seeded "developer" user and exercises the module's
 * enabled HTTP surface end to end. Fixtures are built via direct
 * [[ModuleName]]Model::create() calls (no Eloquent factory is assumed to
 * exist for generated modules) through the fixture helper below.
 */
class [[ModuleName]]CrudTest extends TestCase
{
    protected function setUp(): void
    {
        parent::setUp();

        Sanctum::actingAs(UsersModel::find(UsersModel::DEVELOPER));
    }

[[testMethods]]
}
