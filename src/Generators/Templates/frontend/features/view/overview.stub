<template>
	<div class="space-y-4">
		<div v-if="!data" class="flex items-center justify-center py-12 text-muted-foreground">
			<component :is="icons.InboxIcon" class="h-8 w-8 mr-2" />
			<span class="text-xs">{{ $t('common.no_info') }}</span>
		</div>
		<template v-else>
			[[sections]]

			<!-- Timeline -->
			<Card class="gap-0 overflow-hidden p-0">
				<div class="px-4 py-3 border-b">
					<span class="text-sm font-semibold">{{ $t('common.timeline') }}</span>
				</div>
				<CardContent class="p-0">
					<div class="grid grid-cols-1 md:grid-cols-2">
						<div class="flex items-center justify-between gap-3 px-4 py-2.5 border-b border-border/60">
							<span class="text-xs text-muted-foreground shrink-0">{{ $t('common.created_at') }}</span>
							<span class="text-xs font-semibold text-right">{{ formatDate(data?.created_at) }}</span>
						</div>
						<div v-if="data?.creator" class="flex items-center justify-between gap-3 px-4 py-2.5 border-b border-border/60">
							<span class="text-xs text-muted-foreground shrink-0">{{ $t('common.created_by') }}</span>
							<span class="text-xs font-semibold text-right">{{ data?.creator?.name }}</span>
						</div>
						<div class="flex items-center justify-between gap-3 px-4 py-2.5 border-b border-border/60">
							<span class="text-xs text-muted-foreground shrink-0">{{ $t('common.updated_at') }}</span>
							<span class="text-xs font-semibold text-right">{{ formatDate(data?.updated_at) }}</span>
						</div>
						<div v-if="data?.updater" class="flex items-center justify-between gap-3 px-4 py-2.5 border-b border-border/60">
							<span class="text-xs text-muted-foreground shrink-0">{{ $t('common.updated_by') }}</span>
							<span class="text-xs font-semibold text-right">{{ data?.updater?.name }}</span>
						</div>
					</div>
				</CardContent>
			</Card>

			<!-- System Information -->
			<Card class="gap-0 overflow-hidden p-0">
				<div class="px-4 py-3 border-b">
					<span class="text-sm font-semibold">{{ $t('common.system_info') }}</span>
				</div>
				<CardContent class="p-0">
					<div class="grid grid-cols-1 md:grid-cols-2">
						<div class="flex items-center justify-between gap-3 px-4 py-2.5 border-b border-border/60">
							<span class="text-xs text-muted-foreground shrink-0">{{ $t('common.id') }}</span>
							<span class="text-xs font-semibold font-mono text-right">{{ data?.id }}</span>
						</div>
						<div v-if="data?.uuid" class="flex items-center justify-between gap-3 px-4 py-2.5 border-b border-border/60">
							<span class="text-xs text-muted-foreground shrink-0">{{ $t('common.uuid') }}</span>
							<span class="text-xs font-semibold font-mono text-right truncate max-w-[60%]" :title="data?.uuid">{{ data?.uuid }}</span>
						</div>
					</div>
				</CardContent>
			</Card>
		</template>
	</div>
</template>

<script setup lang="ts">
import { Card, CardContent } from '@/components/ui/card'
import * as icons from 'lucide-vue-next'
[[formatDateImport]]

defineProps<{
	data: any
}>()
</script>
