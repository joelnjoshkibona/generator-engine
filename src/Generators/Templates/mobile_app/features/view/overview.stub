<template>
	<CardSkeleton v-if="loading" :cards="3" :hasHeader="true" />
	<template v-else>
		<div v-if="!data" class="flex items-center justify-center py-12 rounded-lg">
			<component :is="icons.InfoIcon" class="h-8 w-8" />
			<span class="ml-2">No information available.</span>
		</div>
		<div v-else class="space-y-4 animate-fade-in">
			<!-- Information -->
			<Card class="gap-0 overflow-hidden p-0">
				<div class="px-4 py-3 border-b">
					<span class="text-sm font-semibold">Information</span>
				</div>
				<CardContent class="p-0">
					<div class="grid grid-cols-1 md:grid-cols-2">
[[sections]]
					</div>
				</CardContent>
			</Card>
[[lineItemsSections]]

			<!-- Timeline -->
			<Card class="gap-0 overflow-hidden p-0">
				<div class="px-4 py-3 border-b">
					<span class="text-sm font-semibold">Timeline</span>
				</div>
				<CardContent class="p-0">
					<div class="grid grid-cols-1 md:grid-cols-2">
						<div class="flex items-center justify-between gap-3 px-4 py-2.5 border-b border-border/60">
							<span class="text-xs text-muted-foreground shrink-0">Created</span>
							<span class="text-xs font-semibold text-right">{{ formatDate(data?.created_at) }}</span>
						</div>
						<div v-if="data?.creator" class="flex items-center justify-between gap-3 px-4 py-2.5 border-b border-border/60">
							<span class="text-xs text-muted-foreground shrink-0">Created By</span>
							<span class="text-xs font-semibold text-right">{{ data?.creator?.name }}</span>
						</div>
						<div class="flex items-center justify-between gap-3 px-4 py-2.5 border-b border-border/60">
							<span class="text-xs text-muted-foreground shrink-0">Last Updated</span>
							<span class="text-xs font-semibold text-right">{{ formatDate(data?.updated_at) }}</span>
						</div>
						<div v-if="data?.updater" class="flex items-center justify-between gap-3 px-4 py-2.5 border-b border-border/60">
							<span class="text-xs text-muted-foreground shrink-0">Updated By</span>
							<span class="text-xs font-semibold text-right">{{ data?.updater?.name }}</span>
						</div>
					</div>
				</CardContent>
			</Card>

			<!-- System Information -->
			<Card class="gap-0 overflow-hidden p-0">
				<div class="px-4 py-3 border-b">
					<span class="text-sm font-semibold">System Information</span>
				</div>
				<CardContent class="p-0">
					<div class="grid grid-cols-1 md:grid-cols-2">
						<div class="flex items-center justify-between gap-3 px-4 py-2.5 border-b border-border/60">
							<span class="text-xs text-muted-foreground shrink-0">ID</span>
							<span class="text-xs font-semibold font-mono text-right">{{ data?.id }}</span>
						</div>
						<div v-if="data?.uuid" class="flex items-center justify-between gap-3 px-4 py-2.5 border-b border-border/60">
							<span class="text-xs text-muted-foreground shrink-0">UUID</span>
							<span class="text-xs font-semibold font-mono text-right truncate max-w-[60%]" :title="data?.uuid">{{ data?.uuid }}</span>
						</div>
					</div>
				</CardContent>
			</Card>
		</div>
	</template>
</template>

<script setup lang="ts">
import { Card, CardContent } from '@/components/ui/card'
import CardSkeleton from '@/components/ui/loading/CardSkeleton.vue'
import * as icons from 'lucide-vue-next'
import { formatDate } from "@/helpers"
[[lineItemsImports]]

// Props - receives data from parent layout
defineProps<{
	data: any
	loading?: boolean
}>()

const emit = defineEmits(['refresh'])
</script>
